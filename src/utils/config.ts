/**
 * Application configuration
 * 
 * @deprecated Import directly from '@/shared/config/env' instead
 * This file is kept for backwards compatibility
 */
import { env } from '@/shared/config/env'

/**
 * @deprecated Use env.api.url instead
 */
export const apiUrl = env.api.url